fun bool write_nl(bool b) =
  let res = write(b) in
  let tmp = write("\n") in
  res

fun bool main() =  
    let x0 = write_nl(true or true) in  
    let x1 = write_nl(true or false) in
    let x2 = write_nl(false or true) in
    write_nl(false or false) 