fun bool main() = 
    let x = read(int) in
    let y = 7 in
    let to = x = y in
    let be = x = 9 in
    let res = to = be | not to & be = True in
    let z = write(res)   // 0 hvis værdi er 7 ellers 1
    in  let w = ~ x + y * x = y * x - x 
    in  write(w)         // altid true
