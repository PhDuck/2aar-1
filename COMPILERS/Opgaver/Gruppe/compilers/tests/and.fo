fun bool write_nl(bool b) =
  let res = write(b) in
  let tmp = write("\n") in
  res

fun bool main() =  
    let x0 = write_nl(true and true) in  
    let x1 = write_nl(false and true) in
    write_nl(x0 && x1)
